version: '3'
services:
  app:
    image: my-capstone:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3333:3333" # Map the container port to your host port
    environment:
      NODE_ENV: staging # Set your environment variables here
    volumes:
      - .:/usr/src/app